import 'package:flutter/material.dart';

class Hedarfut extends StatelessWidget {
  const Hedarfut({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
